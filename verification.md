# Permanent-site verification

The managed GridPoint preview was checked at 1440×900 and 390×844. The desktop dashboard renders the demand controls, model metrics, client-side city planning plot, hub labels, and decision tabs without overflow or visible runtime errors. The mobile view stacks the control rail cleanly and maintains usable table, slider, and select controls in the initial viewport. The self-contained production build completed successfully before the visual pass.
